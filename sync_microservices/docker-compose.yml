services:
  sync-service:
    build: .
    container_name: jcandy-sync-service
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    restart: always
